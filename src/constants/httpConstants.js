const HTTP_METHODS = {
    GET: "get",
    POST: "post",
    DELETE: "delete",
    PATCH: "patch",
    PUT: "put"
}

const HTTP_CODES = {
    ERR_BAD_REQUEST: "ERR_BAD_REQUEST"
}

const httpConstants = {
    HTTP_METHODS,
    HTTP_CODES
}

export default httpConstants;